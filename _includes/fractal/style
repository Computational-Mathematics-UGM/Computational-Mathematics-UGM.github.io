  <style>
  /* Minimal styles — tweak to integrate with your design */
  .hero {
    position: relative;
    width: 100%;
    height: 100vh;                /* change to 100vh or fixed px if you want */
    /* min-height: 300px; */
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #0b0b0b;         /* fallback background */
    color: white;
  }

  .hero-content {
    position: relative;
    z-index: 2;                  /* content sits above fractal */
    text-align: center;
    pointer-events: none;        /* allow clicks to pass to canvas unless you want otherwise */
  }

  .fractal-canvas {
    position: relative;
    inset: 0;
    width: 200%;
    height: 200%;
    display: block;
    z-index: 1;
    margin-bottom: -50%;
  }
</style>