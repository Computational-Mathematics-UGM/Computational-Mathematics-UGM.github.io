{
  "name": "academic-dept-site",
  "scripts": {
    "merge:bib": "node merge-bib.js",
    "build:css": "tailwindcss -i ./assets/css/style.css -o ./assets/css/main.css --watch",
    "build": "npm run merge:bib && bundle exec jekyll build",
    "serve": "npm run merge:bib && bundle exec jekyll serve"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  },
  "dependencies": {
    "build": "^0.1.4"
  }
}
